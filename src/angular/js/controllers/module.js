/**
* @module definition (controllers/module.js)
* --
* @description It’s just like top level app.js: it defines a module.
*/

define(['angular'], function (ng) {
  'use strict';
  return ng.module('app.controllers', []);
});